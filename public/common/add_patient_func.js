function checkAgeToBday(age, bday){

}

$(document).ready(function(){
    $('#bday').on('change', function() {
        let bday = parseInt($(this).val());
        let checkAge = $("#age").val();
        let age = checkAge ? 
        lockBack = start === 1 || start === 0 ? true : false;
        checkAgeToBday(age, bday);
    });
    
});//doc

  